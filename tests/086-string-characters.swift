// Iterating a string by Character, and its count.
let word = "Swift"
for c in word { print(c, terminator: "|") }
print()
print(word.count, word.first!, word.last!, Array(word))
print(String(word.reversed()))
